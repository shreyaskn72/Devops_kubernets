apiVersion: v1
kind: Service
metadata:
  name: celery-worker
spec:
  type: ClusterIP
  selector:
    app: celery-worker
  ports:
    - port: 5555
      targetPort: 5555
      protocol: TCP
      name: metrics


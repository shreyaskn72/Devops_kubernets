apiVersion: v1
kind: Service
metadata:
  name: flower-service
spec:
  type: ClusterIP
  selector:
    app: flower
  ports:
    - port: 5555
      targetPort: 5555
      protocol: TCP
      name: metrics


apiVersion: v1
kind: Service
metadata:
  name: flower
spec:
  type: {{ .Values.service.type }}
  selector:
    app: flower
  ports:
    - port: 5555
      targetPort: 5555
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http


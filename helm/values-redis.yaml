architecture: standalone

auth:
  enabled: false

master:
  persistence:
    enabled: true
    storageClass: hostpath
    size: 1Gi

resources:
  requests:
    memory: 128Mi
    cpu: 100m

  limits:
    memory: 256Mi
    cpu: 200m


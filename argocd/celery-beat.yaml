apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: celery-beat
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/shreyaskn72/Devops_kubernets.git
    targetRevision: master
    path: helm/celery-beat

    helm:
      valueFiles:
        - values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: celery-beat

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true